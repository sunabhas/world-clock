<template>
  <button
    type="button"
    :disabled="disabled"
    @click="handleButtonClick"

    :class=" !disabled ? 'text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800': 'text-white bg-gray-700 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2'"
  >
    {{ text }}
  </button>
</template>

<script setup>

const props = defineProps(['text', 'disabled']);
const emits = defineEmits(['handleClick']);

const handleButtonClick = () => {
  emits('handleClick'); 
};
</script>
